import type { NextPage } from 'next';
import DomainForm from '../components/DomainForm';

const Home: NextPage = () => {
    return (
        <div>
            <DomainForm />
        </div>
    );
};

export default Home;
